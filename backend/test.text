// let sql = "SELECT * FROM groupomania.articles";
//     let values = [req.body.image];
//     console.log(req.body)
//     db.query(sql, values, function (err, result) {
//         if (err) throw err;
//         console.log(result.affectedRows + " record(s) updated");
//         return res.status(201).json({ result })
//     });
//     if (req.file) {
        //select pour récupèrer l'image
        

        /* if selui qui change l'image est la créateur d'image {
            const 
        }
        */

        // const imageUrl = (`${req.protocol}://${req.get("host")}/images/${req.file.filename}`);

        // const filename = req.file ? sauce.imageUrl.split('/images/')[1] : "";//je récupère l'adresse
        // fs.unlink(`images/${filename}`, ()=>{
        //     Sauce.uptateOne()//ubdade nuvelle image
        //     .then(()=> res.status(200).json({message: 'image modifié !'}))
        // })


        
        // const sql = "UPDATE groupomania.articles SET image = ?  WHERE id_article = ?";
        
        // console.log(req.file);

        // const image = req.file.filename;
        
        // On supprime l'ancienne image du serveur
        // const image = imagePath.split('/images/')[1];
        // fs.unlinkSync(`images/${image}`);

        // let values = [image, req.body.id_article];
  
        // db.query(sql, values, function (err, result) {
            
        //     if (err) throw err;
        //     console.log(result.affectedRows + " record(s) updated");
        //     return res.status(201).json({ result })
        // });


            let sql = "SELECT * FROM groupomania.articles WHERE image LIKE '%jpg%' Or id_article = ?";
   
    let values = [req.body.id_article, req.body.image];
    db.query(sql, values, function (err, result) {
        console.log(result)
        if (err) throw err;
        console.log(result.affectedRows + " record(s) updated");
        return res.status(201).json({ result })
    });